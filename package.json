{
	"name": "simple-js-api",
	"version": "1.0.0",
	"description": "",
	"main": "api.js",
	"scripts": {
		"start": "netlify-lambda serve src",
		"build": "set NODE_OPTIONS=--openssl-legacy-provider netlify-lambda build src",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Steven Geudens",
	"license": "MIT",
	"dependencies": {
		"dotenv": "16.0.3",
		"express": "4.18.2",
		"mongoose": "6.6.5",
		"serverless-http": "3.0.3",
		"netlify-lambda": "2.0.15"
	},
	"devDependencies": {
		"nodemon": "2.0.20"
	}
}
